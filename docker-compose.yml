version: '3'
services:
  app:
    build: 
      context: .
      dockerfile: "dockerfile"
    ports: 
      - 3000:3000
    volumes: 
      - "./src/:/opt/src/"
      - "./bin/:/opt/bundle/"
    expose: 
      - "3000"